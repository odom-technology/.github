# Workflow templates

Reusable organization workflow templates belong here after the validation toolchain
for each repository type is selected. Every template must document permissions,
third-party actions, secrets, triggers, and expected maintenance ownership.
